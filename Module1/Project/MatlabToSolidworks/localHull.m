% uses a predefined mesh and an interpolation method to define the design
% returns the distance from the deck surface to the bottom of the boat
% if outside the boat, return 0
function z = localHull(x, y, X, Y, A, params)

end